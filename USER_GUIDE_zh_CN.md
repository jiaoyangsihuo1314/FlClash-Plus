# FlClash Plus 客户端使用教程

本文在同一篇文档中分别介绍 Android、Windows 和 macOS 客户端。三个章节相互独立，用户只需阅读自己设备对应的章节即可完成安装、连接、日常使用和故障排查。

FlClash Plus 是代理客户端。使用 AI68 一键连接需要有效账号；使用手动模式需要准备兼容 Clash/Mihomo 的订阅地址、二维码或 YAML 配置文件。

## 一、Android 客户端

### 1. 下载与安装

1. 从可信的官方发布渠道下载 APK。
2. 大多数新款手机选择 `arm64-v8a`；较旧的 32 位手机选择 `armeabi-v7a`；模拟器可能需要 `x86_64`。
3. 不确定设备架构时，优先下载通用版。
4. 如果系统阻止安装，请临时允许当前浏览器或文件管理器“安装未知应用”。
5. 完成安装后，建议关闭“安装未知应用”权限。
6. 打开 FlClash Plus，阅读并确认免责声明。

### 2. 选择使用方式

首次进入欢迎页面时，可以选择：

- **登录AI68账号**：登录后选择地区并一键连接。
- **进入原FlClash功能**：导入自己的订阅或配置文件。

### 3. 使用 AI68 一键连接

1. 点击“登录AI68账号”。
2. 输入邮箱和密码，然后点击“登录”。
3. 没有账号时，点击“创建账号”，按页面提示填写邮箱、密码、验证码或邀请码。
4. 登录后进入“AI68中心”。
5. 选择“智能选择”“美国”“日本”或“香港”。日常使用建议选择“智能选择”。
6. 点击“一键连接”。
7. 第一次连接时，Android 会显示系统 VPN 请求，点击允许。
8. 等待页面显示“已连接”。状态栏出现 VPN 图标后，表示系统流量已由 FlClash Plus 接管。

需要停止时，返回“AI68中心”并点击“停止代理”。

AI68 中心还可以查看账号、套餐、订单、节点、通知、剩余流量和到期时间。在“我的”页面可以查看用于消费的账户余额，也可以输入旧密码和新密码修改 AI68 登录密码。右上角刷新按钮用于更新账号信息，退出按钮用于注销账号。

### 4. 手动导入订阅

1. 在欢迎页点击“进入原FlClash功能”。
2. 进入“配置”页面。
3. 点击右下角“添加配置”。
4. 选择导入方式：
   - 二维码：调用相机扫描订阅二维码，需要允许相机权限。
   - 文件：选择手机中的 YAML 配置文件。
   - URL：粘贴订阅地址并提交。
5. 导入成功后，点击配置卡片，使其显示为选中状态。

只有选中有效配置后，“代理”页面和仪表盘启动按钮才会显示。

配置卡片右上角菜单可以编辑名称和订阅地址、开启自动更新、立即同步、预览、导出或删除配置。配置页顶部的同步按钮可以更新全部 URL 订阅。

### 5. 选择节点和模式

1. 进入“代理”页面。
2. 打开需要使用的代理组。
3. 点击目标节点。
4. 点击右下角测速按钮，测试当前组内节点延迟。
5. 使用顶部搜索框查找节点。
6. 在右上角“设置”中调整节点排序和页面样式。

返回“仪表盘”，在“出站模式”中选择：

- **规则**：按订阅规则分流，推荐日常使用。
- **全局**：所有流量都使用代理。
- **直连**：流量不经过代理。

### 6. 开启 Android VPN

1. 在仪表盘找到“VPN”组件并打开开关。
2. 如果组件未显示，点击仪表盘右上角编辑按钮，将“VPN”添加回来并保存。
3. 点击右下角播放按钮启动代理。
4. 按钮显示运行时长后，表示代理正在运行。
5. 再次点击播放按钮即可停止。

Android 通常只能同时运行一个系统 VPN。启动 FlClash Plus 前，应先关闭其他 VPN 应用。

### 7. 按应用设置代理

1. 进入“工具 -> 访问控制”。
2. 选择访问控制模式。
3. 勾选需要代理或需要排除的应用。
4. 返回仪表盘。
5. 停止并重新启动 VPN，使设置生效。

如果某个应用无法联网，先检查它是否被访问控制意外排除。

### 8. 防止后台断开

1. 进入“工具 -> 进阶配置 -> 按需连接”。
2. 按提示关闭 FlClash Plus 的电池优化。
3. 在手机系统设置中允许后台运行和自启动。
4. 部分品牌手机还需要在最近任务中锁定应用。
5. 修改权限后重新启动 VPN。

### 9. 更新和切换节点

- 更新单个订阅：配置卡片菜单 -> “同步”。
- 更新全部订阅：配置页顶部同步按钮。
- 自动更新：配置卡片菜单 -> “编辑” -> 开启“自动更新”。
- 切换节点：进入“代理”并点击新节点。
- 关闭旧连接：进入“连接”页面手动关闭，或在“工具 -> 应用程序”中开启切换节点后自动关闭连接。

### 10. Android 备份与恢复

进入“工具 -> 备份与恢复”。

本地备份：

1. 点击“本地 -> 备份”。
2. 选择保存位置。
3. 恢复时点击“本地 -> 恢复”。
4. 选择备份文件，再选择“仅恢复配置文件”或“恢复所有数据”。

WebDAV 备份：

1. 点击“绑定”。
2. 填写 WebDAV 地址、账号和密码。
3. 等待连接状态正常。
4. 点击“备份”上传，或点击“恢复”下载数据。

### 11. Android 常见问题

**导入后没有“代理”页面**

返回“配置”页面，点击配置卡片并确认已选中。如果仍未出现，重新同步订阅并检查配置预览中是否包含节点。

**点击启动后没有 VPN 图标**

确认已打开仪表盘的 VPN 开关，并在 Android 系统 VPN 对话框中点击允许。若权限被拒绝，可到系统 VPN 设置中重新授权。

**锁屏一段时间后断开**

关闭电池优化，允许后台运行和自启动，并检查系统是否自动清理了 FlClash Plus。

**部分应用无法使用代理**

检查“访问控制”，确认应用没有被排除。修改后重新启动 VPN。

**启动后完全无法联网**

确认不是“直连”模式，切换其他节点并测速，停止其他 VPN，随后重新启动 FlClash Plus。

**应用异常退出后仍显示 VPN**

进入 Android 系统的 VPN 设置，手动断开 FlClash Plus。

## 二、Windows 客户端

### 1. 下载与安装

1. 从可信的官方发布渠道下载 Windows EXE 安装包。
2. 运行安装程序并按提示完成安装。
3. 从开始菜单启动 FlClash Plus。
4. 如果 Windows 防火墙询问是否允许访问网络，建议仅在专用网络中允许。
5. 阅读并确认免责声明。

建议使用完整 EXE 安装包。ZIP 便携版可以用于普通系统代理，但虚拟网卡和相关服务使用完整安装版更加稳妥。

### 2. 选择使用方式

欢迎页面提供两种方式：

- **登录AI68账号**：登录后直接选择地区并连接。
- **进入原FlClash功能**：手动导入自己的订阅或配置。

### 3. 使用 AI68 一键连接

1. 点击“登录AI68账号”。
2. 输入邮箱和密码。
3. 没有账号时，点击“创建账号”完成注册。
4. 登录后进入“AI68中心”。
5. 选择“智能选择”“美国”“日本”或“香港”。
6. 点击“一键连接”。
7. 等待客户端完成同步和节点选择。
8. 页面显示“已连接”后即可使用。

需要停止时，点击“停止代理”。AI68 中心还可以查看套餐、订单、节点、通知、流量和到期时间。在“我的”页面可以查看用于消费的账户余额并修改 AI68 登录密码。

### 4. 手动导入订阅

1. 在欢迎页点击“进入原FlClash功能”。
2. 进入“配置”。
3. 点击右下角“添加配置”。
4. 选择导入方式：
   - 二维码：选择包含订阅二维码的本地图片。
   - 文件：选择 YAML 配置文件。
   - URL：粘贴订阅地址并提交。
5. 点击导入后的配置卡片，使其成为当前配置。

配置卡片菜单支持编辑、预览、同步、复制链接、导出和删除。开启自动更新时，可以设置更新间隔。

### 5. 选择节点和模式

1. 进入“代理”。
2. 在目标代理组中点击需要使用的节点。
3. 点击右下角测速按钮测试节点延迟。
4. 使用顶部搜索框查找节点。
5. 在右上角“设置”中调整排序和显示方式。

在“仪表盘”的“出站模式”中选择：

- **规则**：按规则分流，推荐日常使用。
- **全局**：所有流量使用代理。
- **直连**：不使用代理。

### 6. 使用 Windows 系统代理

系统代理适合浏览器和大多数遵循 Windows 代理设置的应用。

1. 在仪表盘打开“系统代理”。
2. 点击右下角播放按钮启动。
3. 按钮显示运行时长后开始使用。
4. 停止时再次点击播放按钮。
5. 不再使用时关闭“系统代理”。

首次使用建议先选择系统代理，确认订阅和节点能够正常工作。

### 7. 使用 Windows 虚拟网卡

游戏、终端程序或不读取系统代理的应用，可以尝试虚拟网卡。

1. 确认当前使用完整 EXE 安装版。
2. 在仪表盘打开“虚拟网卡”。
3. 根据提示同意管理员授权。
4. 点击播放按钮启动。
5. 如果无法联网，先关闭其他 VPN、代理客户端或网络过滤软件。

如果提示 Helper 服务不可用：

1. 退出 FlClash Plus。
2. 使用最新完整安装包重新安装。
3. 检查安全软件是否隔离了 FlClash Plus 文件。
4. 重新启动 Windows 后再试。

### 8. Microsoft Store 应用无法联网

部分 Microsoft Store 应用可能无法访问本地代理：

1. 进入“工具”。
2. 点击“Loopback”。
3. 同意管理员授权。
4. 为需要使用代理的应用解除回环限制。

也可以改用虚拟网卡接管更多应用流量。

### 9. 托盘、自启动和静默启动

进入“工具 -> 应用程序”，可以设置：

- 跟随 Windows 启动。
- 静默启动。
- 打开客户端后自动运行代理。
- 最小化和关闭窗口行为。
- 切换节点后自动关闭旧连接。

关闭主窗口后，客户端可能仍在系统托盘运行。需要完全退出时，请使用托盘菜单中的退出功能。

### 10. Windows 备份与恢复

进入“工具 -> 备份与恢复”。

本地备份：

1. 点击“本地 -> 备份”。
2. 选择保存位置。
3. 恢复时选择“本地 -> 恢复”和对应备份文件。
4. 选择只恢复配置或恢复全部数据。

WebDAV 备份：

1. 绑定 WebDAV 地址、账号和密码。
2. 确认连接状态正常。
3. 点击“备份”上传，或点击“恢复”下载。

### 11. Windows 常见问题

**打开系统代理后无法上网**

重新打开 FlClash Plus 并关闭“系统代理”。必要时进入 Windows“设置 -> 网络和 Internet -> 代理”，关闭手动代理。

**浏览器能用，但游戏或命令行不能用**

这些程序可能不读取系统代理。确认普通节点可用后，再尝试虚拟网卡。

**虚拟网卡开启后断网**

停止代理并关闭虚拟网卡，关闭其他 VPN 或网络过滤软件，先验证系统代理正常，再重新尝试。

**切换节点后 IP 没有变化**

进入“连接”页面关闭旧连接，或开启“切换节点后自动关闭连接”，然后重新打开目标程序。

**订阅更新失败**

检查订阅是否到期、地址是否正确以及网络是否正常。部分订阅需要指定 User-Agent，可在“工具 -> 基本配置 -> User-Agent”中调整。

**退出后网络异常**

检查系统代理是否仍开启。如果客户端仍在托盘运行，从托盘菜单完全退出或先关闭系统代理。

## 三、macOS 客户端

### 1. 下载与安装

1. 从可信的官方发布渠道下载 DMG。
2. Apple 芯片 Mac 选择 `arm64`，Intel Mac 选择 `amd64` 或 `x64`。
3. 打开 DMG，将 FlClash Plus 拖入“应用程序”文件夹。
4. 从“应用程序”中启动 FlClash Plus。
5. 如果 macOS 阻止首次打开，请进入“系统设置 -> 隐私与安全性”，确认允许打开。
6. 阅读并确认免责声明。

### 2. 选择使用方式

欢迎页面可以选择：

- **登录AI68账号**：选择地区并一键连接。
- **进入原FlClash功能**：导入自己的订阅或配置文件。

### 3. 使用 AI68 一键连接

1. 点击“登录AI68账号”。
2. 输入邮箱和密码。
3. 没有账号时，点击“创建账号”并完成注册。
4. 登录后进入“AI68中心”。
5. 选择“智能选择”“美国”“日本”或“香港”。
6. 点击“一键连接”。
7. 等待客户端完成同步和节点选择。
8. 页面显示“已连接”后即可使用。

需要停止时，点击“停止代理”。账号、套餐、订单、节点、通知、流量和到期时间也可以在 AI68 中心查看。在“我的”页面可以查看用于消费的账户余额并修改 AI68 登录密码。

### 4. 手动导入订阅

1. 在欢迎页点击“进入原FlClash功能”。
2. 进入“配置”。
3. 点击右下角“添加配置”。
4. 选择导入方式：
   - 二维码：选择包含订阅二维码的本地图片。
   - 文件：选择 YAML 配置文件。
   - URL：粘贴订阅地址并提交。
5. 点击导入后的配置卡片，使其显示为选中状态。

配置卡片菜单支持编辑、预览、同步、复制链接、导出和删除。配置页顶部可以批量更新订阅或调整配置顺序。

### 5. 选择节点和模式

1. 进入“代理”。
2. 在目标代理组中点击节点。
3. 点击右下角测速按钮测试延迟。
4. 使用顶部搜索框查找节点。
5. 在右上角“设置”中调整排序和显示方式。

在“仪表盘”的“出站模式”中选择：

- **规则**：按照订阅规则分流，推荐日常使用。
- **全局**：所有流量使用代理。
- **直连**：不使用代理。

### 6. 使用 macOS 系统代理

系统代理适合 Safari、Chrome 和大多数常规应用。

1. 在仪表盘打开“系统代理”。
2. 点击右下角播放按钮启动。
3. 按钮显示运行时长后开始使用。
4. 停止时再次点击播放按钮。
5. 不再使用时关闭“系统代理”。

首次使用建议先测试系统代理，确认订阅和节点可用。

### 7. 使用 macOS 虚拟网卡

终端程序、游戏或不读取系统代理的应用，可以尝试虚拟网卡。

1. 在仪表盘打开“虚拟网卡”。
2. 根据 macOS 提示完成管理员授权。
3. 点击播放按钮启动。
4. 如果无法联网，关闭其他 VPN、代理或网络过滤软件后重试。
5. 修改虚拟网卡、DNS 或路由设置后，停止并重新启动代理。

### 8. 自启动和后台运行

进入“工具 -> 应用程序”，可以设置：

- 跟随 macOS 启动。
- 静默启动。
- 打开客户端后自动运行代理。
- 最小化和关闭窗口行为。
- 切换节点后自动关闭旧连接。

关闭主窗口后，客户端可能仍在菜单栏运行。需要完全退出时，请使用菜单栏图标中的退出功能。

### 9. macOS 备份与恢复

进入“工具 -> 备份与恢复”。

本地备份：

1. 点击“本地 -> 备份”。
2. 选择保存位置。
3. 恢复时点击“本地 -> 恢复”。
4. 选择备份文件以及恢复范围。

WebDAV 备份：

1. 绑定 WebDAV 地址、账号和密码。
2. 等待连接状态正常。
3. 点击“备份”上传，或点击“恢复”下载。

### 10. macOS 常见问题

**开启系统代理后无法上网**

重新打开 FlClash Plus 并关闭“系统代理”。如果客户端无法打开，可进入 macOS 网络设置，手动关闭 HTTP、HTTPS 和 SOCKS 代理。

**Safari 可以使用，但终端或其他应用不能使用**

这些应用可能不读取系统代理。可以为终端单独设置代理，或在确认节点正常后尝试虚拟网卡。

**虚拟网卡无法启用**

确认已经完成管理员授权，关闭其他 VPN 和网络过滤软件，然后停止并重新启动 FlClash Plus。

**切换节点后出口 IP 没有变化**

进入“连接”页面关闭旧连接，或开启切换节点后自动关闭连接，再重新打开浏览器或目标应用。

**订阅更新失败**

检查订阅地址、账号有效期和当前网络。必要时在“工具 -> 基本配置 -> User-Agent”中设置订阅服务要求的值。

**客户端退出后网络异常**

确认应用是否仍在菜单栏运行，并检查 macOS 网络设置中的系统代理是否已关闭。

## 四、三端通用的安全提示

- 不要向他人公开订阅地址、节点密码、账号令牌或备份文件。
- 分享日志和截图前，遮挡邮箱、IP、订阅地址和节点信息。
- 不要导入来源不明的配置或脚本。
- 只在可信网络中开启“允许局域网”。
- 公共设备使用完毕后，应退出账号、停止代理并删除本地配置。
- 使用代理服务时，请遵守所在地法律法规和服务提供方的使用条款。
